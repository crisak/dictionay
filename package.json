{
  "name": "dictionary",
  "private": true,
  "scripts": {
    "build:dev": "turbo run build:dev --filter sls-api --filter admin",
    "build:sls-api:dev": "cd apps/sls-api && yarn build:dev",
    "build": "turbo run build --filter sls-api --filter admin",
    "dev": "turbo run dev --filter sls-api --filter admin",
    "lint": "turbo run lint --filter sls-api --filter admin",
    "format": "turbo run format --filter sls-api --filter admin",
    "check-types": "turbo run check-types --filter sls-api --filter admin"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "5.8.2",
    "@types/node": "^22.14.0"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
